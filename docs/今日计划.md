# 今日计划 — 2026-05-22

## 一、当前状态概览

| 模块 | 状态 | 说明 |
|------|------|------|
| 文本输入 | ✅ 完成 | CLI (`src/main.py`) + Web (`src/server.py` + React 前端) |
| AI意图识别 | ✅ 完成 | 7种意图，Prompt模板化 |
| AI记账 | ✅ 完成 | 支出/收入解析 |
| AI日程 | ✅ 完成 | 日程解析+提醒 |
| JSON结构化输出 | ✅ 完成 | json_utils 校验与清理 |
| 数据存储 | ❌ 未完成 | **内存存储，非SQLite** |
| 测试 | ❌ 未完成 | tests/ 目录为空 |
| 多事务拆分 | ✅ 完成 | 同类+混合类型均支持，规则+AI两阶段拆分 |
| 语音输入 | ❌ 未实现 | UI有按钮但无功能 |
| 修改/删除 | ⚠️ 部分 | 已实现但未充分测试 |

---

## 二、今日任务

### 任务1：SQLite 持久化存储（核心）

**背景：** REQUIREMENTS.md 明确要求 SQLite 存储，PROJECT_DESIGN.md 也指定 SQLite 为前期数据库。当前所有数据存储在 `src/repositories/entry_repository.py` 的进程内列表中，重启即丢失。

**工作内容：**
- 创建 SQLite 数据库初始化脚本（三张表：expense、income、schedule）
- 改造 `entry_repository.py`，将内存 CRUD 替换为 SQLite 操作
- 保留现有函数签名，确保 `server.py` 和 `main.py` 无需改动

**涉及文件：**
- 新建 `src/database/` 目录 + 初始化脚本
- 修改 `src/repositories/entry_repository.py`

---

### 任务2：补充基础测试

**背景：** tests/ 目录只有空的 `__init__.py`。MVP 已可运行，但缺乏回归保护。

**工作内容：**
- 编写时间解析工具 `time_utils.py` 的单元测试（纯函数，不依赖 LLM）
- 编写 JSON 解析工具 `json_utils.py` 的单元测试
- 编写 `entry_repository.py` 的 CRUD 测试（需先完成任务1）

**涉及文件：**
- 新建 `src/tests/test_time_utils.py`
- 新建 `src/tests/test_json_utils.py`
- 新建 `src/tests/test_entry_repository.py`

---

### 任务3（可选）：多事务拆分验证

**背景：** PROMPTS.md 和 PROJECT_DESIGN.md 都提到多事务拆分（如"今天午饭35，晚上打车20"），Prompt 已定义但未充分验证实际效果。

**工作内容：**
- 用 PROMPTS.md 中的测试案例逐一验证当前系统输出
- 如发现解析错误，调整 extract_service.py 或 Prompt

---

## 三、执行顺序

```
任务1（SQLite存储）
  ↓
任务2（测试）依赖任务1完成后进行 repository 测试
  ↓
任务3（多事务验证）可并行
```

## 四、预期产出

1. SQLite 数据库文件，重启后数据不丢失
2. 至少 3 个测试文件，覆盖核心工具函数和存储层
3. （可选）多事务拆分测试报告或修复

## 五、今日不做

- 语音输入（V3 范畴）
- Controller 层拆分（架构优化，非 MVP 阻塞项）
- Prompt 优化/Few-shot（需多轮实验）
- 推送通知、云同步、多用户
